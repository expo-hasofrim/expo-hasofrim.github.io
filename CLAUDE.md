# אקספו הסופרים — הקשר לעבודה על הפרויקט

מדריך מידע ועסקים מודפס למתחם הסופרים, צפון בני ברק. אתר סטטי בשני
עמודים שמשרת אותו: דף נחיתה שמוכר פרסום לבעלי עסקים, ודף אלפון שרושם
תושבים.

**חי ב־** https://expo-hasofrim.github.io/
**מאגר:** ארגון `expo-hasofrim` (ארגון ולא פרופיל אישי, כדי לקבל דומיין
שורש נקי).

---

## מבנה

| קובץ | תפקיד |
|---|---|
| `index.html` | דף הנחיתה — מוכר פרסום. מחירון, סליקה, פנייה לעסקים. |
| `alfon.html` | טופס רישום תושבים לאלפון + שאלות נפוצות. |
| `signature.html` | כלי פנימי: חתימת מייל להעתקה ל־Gmail. `noindex`. |
| `apps-script/Code.gs` | הקוד שרץ בגיליון גוגל וקולט את הרישומים. |
| `assets/` | לוגו, תמונת שכונה, לוגו לחתימה, אייקוני טלפון/וואטסאפ. |
| `handoff/` | **לא במעקב git.** תוצרים ללקוח — מדריך התקנה, פרוספקט, קוד כטקסט. |

**אין שרת ואין build.** HTML/CSS/JS גולמי בקובץ אחד לכל דף. אין תלויות,
אין npm, אין תהליך בנייה. עורכים ודוחפים.

**פרטים פרטיים** (כתובת הגיליון, זהויות חשבונות) נמצאים ב־`NOTES-private.md`
המקומי, שלא נכנס ל־git כי המאגר ציבורי.

---

## דיפלוי

`git push origin main` ← GitHub Pages מגיש תוך **1–4 דקות**.

**תמיד לאמת מול החי, לא מול הקובץ המקומי:**

```bash
curl -s https://expo-hasofrim.github.io/ | grep -c "מה ששיניתם"
```

בדיקה מדפדפן מחובר עלולה לשקר בגלל מטמון. `curl` הוא האמת.

---

## אנשים

- **הבעלים של האקספו** — שמו אינו ידוע. הוא מוסר בקשות דרך בנימין.
- **שמחה** — **המנהל, לא הבעלים.** הוא תיווך את העבודה ואין לו קשר ישיר.
  אל תניח שהוא מקבל ההחלטות ואל תפנה אליו בשם בטיוטות.
- **בנימין** — המפתח. כל בקשה מגיעה דרכו.

**בחומרים ללקוח לא כותבים שמות של אנשים** אלא אם התבקש במפורש.

---

## צינור הרישום לאלפון

```
טופס ב-alfon.html  ──POST──►  Apps Script  ──►  גיליון גוגל
        │                                              │
        └── נכשל? ──► נופל לוואטסאפ (972504181682) ──► הודעה ידנית
```

הכתובת יושבת במשתנה `ENDPOINT` בראש הסקריפט של `alfon.html`.
אם הוא ריק או שהבקשה נכשלת — הטופס נופל חזרה לוואטסאפ, כך ש**שום רישום
לא הולך לאיבוד** גם כשהגיליון מושבת.

### שלוש מלכודות שעלו ביוקר

**1. `Content-Type` חייב להיות `text/plain`, לא `application/json`.**
JSON מפעיל preflight שגוגל לא עונה עליו והבקשה נחסמת. `text/plain` היא
simple request ועוברת. הגוף עצמו עדיין `JSON.parse` בצד השני.

**2. `appendRow` אוכל אפס מוביל.** עיצוב העמודה כ"טקסט רגיל" **לא מספיק** —
appendRow ממיר מחרוזת שנראית כמו מספר למספר, בלי קשר לעיצוב התא, ו־
`037654321` נשמר כ־`37654321`. הפתרון בקוד: `pickText()` שמוסיף גרש מוביל.
**איך מזהים:** לסמן את עמודת הטלפון — אם שורת הסטטוס מציגה "סה״כ", הערכים
מספריים והאפס אבד.

**3. עריכת הקוד לבדה לא משנה כלום.** כתובת `/exec` מגישה **גרסה קפואה**.
כל תיקון דורש:
`פריסה ← ניהול הפריסות ← עיפרון ← גרסה: חדשה ← פריסה`.
הכתובת עצמה לא משתנה.

### הגיליון

לשונית **`רישומים`** בלבד. 14 עמודות: תאריך קבלה · סוג הפנייה · שם משפחה ·
שם האיש · שם האשה · רחוב · בניין · כניסה · קומה · דירה · טלפון בבית ·
נייד הבעל · נייד האשה · מעמד.

הקוד תלוי ב**שם הלשונית** וב**שורת הכותרות**. שינוי שלהם שובר את הקליטה.

רוחבי העמודות מכווננים כך שכל 14 נכנסות למסך אחד (A–F ברוחב 118,
G–J ברוחב 65, A ברוחב 140). **אין מסנן משותף בכוונה** — מסנן שהופעל
בטעות מסתיר שורות ונראה כאילו נתונים אבדו.

---

## עובדות תוכן שאסור לבלבל

**שני תאריכים נפרדים:**
- **המדריך (האקספו)** יוצא לאור **בתשפ״ז**, בלי תאריך מדויק, ו**בלי האלפון**.
- **אלפון התושבים** מתוכנן ל**קיץ תשפ״ז** בלבד.

**שני מספרי טלפון, מחולקים לפי ערוץ:**
- **וואטסאפ ← 053-8504980** — רק קישורי `wa.me` בדף הנחיתה.
- **טלפון ← 050-4181682** — חיוג, טקסט מוצג, `schema.org`, וכל דף האלפון
  כולל ה־WA fallback של הטופס.

*(הוצע לאחד ונדחה במפורש. לא להציע שוב.)*

**השכונה היא שני רחובות בלבד:** `600 — שבט הלוי` ו־`601 — חזון עובדיה`.
המספרים 600/601 הם **שמות הרחובות**, לא מספרי טלפון.

**כתיב:** בדף כתוב `האשה` ולא `האישה`. לשמור על עקביות.

---

## מידות דפוס ומחירון

הדף כולו **21.5×15 ס״מ**, ו**שטח הפרסום שבתוכו 17.5×12**.
כל המידות באתר הן של שטח הפרסום. מוסכמה: אופציה "לרוחב" נכתבת
רוחב×גובה, "לגובה" נכתבת גובה×רוחב.

| דרגה | מידות | מחיר | שטח |
|---|---|---|---|
| עמוד שלם | 17.5×12 | 1,200 ₪ | 1/1 |
| חצי עמוד | לגובה 17.5×6 · לרוחב 12×8.5 | 620 ₪ | 1/2 |
| שליש עמוד | לרוחב 12×5.8 | 500 ₪ | 1/3 |
| רבע עמוד | 8.5×6 | 380 ₪ | 1/4 |
| שמינית עמוד | 6×4 | 220 ₪ | 1/8.8 |
| שורה באינדקס | — | 100 ₪ | — |

תוספת גרפיקה 50 ₪ · התאמה למדריך חינם.
סליקה: **נדרים פלוס**, `matara.pro/nedarimplus/online/?mosad=7018890`,
קישור נפרד לכל דרגה עם `Avour` מתאר.

### שתי מלכודות במחירון

**המידות חייבות לתת את החלק שהשם מבטיח.** פעם נמסרו מידות שגויות
(`9×13` לשליש, שחרג מרוחב העמוד) ורק החישוב תפס את זה.
בדיקה: `שטח == 210/מכנה`.

**פריט יתום נקרא כהמלצה.** כשמספר הדרגות לא מתחלק ברוחב השורה,
האחרונה נשארת לבד במרכז ונראית כמומלצת — וזה קרה דווקא עם הזולה ביותר.
ההגנות בקוד:
- `.tier.line` (שורה באינדקס) מקבלת **שורה משלה בכוונה**, כרצועה שטוחה
  בלי מילוי — היא ממילא מוצר אחר (רישום טקסט, לא מודעה).
- בנייד `max-width:calc(50% - 7px)` מונע מהכרטיס האחרון להימתח.

**אחרי כל שינוי במחירון — לבדוק פריסה בדסקטופ ובנייד.**

---

## שפת העיצוב

```
--ink:#1B2A4A   --ink-deep:#131F38   --ink-dark:#0C1526   --muted:#5A6478
--gold:#C9A24B  --gold-light:#E5C878 --gold-deep:#8F6A1F
--bg:#FAFBFD    --mist:#EFF2F8       --mist-line:#E0E5EF
--grad-gold: linear-gradient(115deg,#E9CF83 0%,#C9A24B 48%,#A97F2E 100%)
```

**גופנים:** `Secular One` לכותרות · `Assistant` לגוף · `Rubik` למספרים.

**אילוצים שנקבעו בכאב:**
- **אפס מיתוג של פוקוס נדל״ן.** הדף שייך לאקספו בלבד.
- **האתר מותאם ללוגו** (נייבי + זהב), לא הפוך.
- **שכונה חרדית צעירה, לא אסתטיקה עתיקה.** הקהל הוא בני 25–45.
  שלוש גרסאות נדחו כי הונח ש"חרדי" שווה "מיושן".

### הלוגו — שתי מלכודות

`assets/expo-logo.png` הוא **512×512 עם שוליים שקופים ענקיים**. הציור
עצמו יושב ב־`(62,157)-(452,371)`, כלומר 390×215. **לחתוך לפני שימוש
בחומרי דפוס**, אחרת נוצר חלל מת.

```python
from PIL import Image
im = Image.open("assets/expo-logo.png").convert("RGBA")
im.crop(im.split()[3].getbbox()).save("out.png")
```

**ללוגו יש שורת תיוג משלו.** היא חלק מהאמנות ונשארת. **אסור להוסיף
שורת תיוג נוספת מתחתיה** — זו שגיאה שחזרה פעמיים.

**הנוסח המחייב, מ-1.9.26: "מדריך מידע ועסקים של שכונת הסופרים".**
הוא נסדר מחדש בשני הקבצים — `expo-logo.png` (האתר) ו-`signature-logo-v3.png`
(החתימה).

**הגופן של שורת התיוג הוא Rubik 400.** זוהה בכך שהנוסח הישן שוחזר בכל
מועמד והושווה לאמנות המקורית; מילה בודדת ("מדריך") מפרידה היטב בין
הגופנים, ומחרוזת שלמה **לא** — סחיפת מיקום מצטברת מרסקת את ה-IoU וכל
המועמדים יוצאים זהים. Rubik הוא ממילא אחד מגופני האתר.

**המתכון לסידור מחדש:** לרנדר ב-Chrome headless ברזולוציה ×10 עם
`--default-background-color=00000000`, לפתור את `font-size` כך שגובה הדיו
יהיה 190, ואת `letter-spacing` כך שהרוחב יהיה פי 10 מהיעד, ואז להקטין
ב-LANCZOS לרצועה של 19px ולכפות RGB שחור תוך שמירת האנטי-אליאסינג באלפא.
המיקום: `signature-logo` שורות 194–212 ומרכז `x=193.5`;
`expo-logo` שורות 350–368 ומרכז `x=255.5` (היסט קבוע של +62/+156).
הנוסח החדש רחב יותר (372px מול 334) — הוא מיושר בערך לרוחב הסמל, וזה מכוון.

---

## הפקת תמונות ו־PDF מעוצבים

**Chrome headless מרנדר webfonts ב־`--screenshot` אבל לא ב־`--print-to-pdf`.**
נוסו כל צירופי הדגלים, כולל הטמעת הגופנים כ־data-URI. לכן:

```bash
# 1240×1754 ב-scale 2 = 2480×3508 = A4 ב-300dpi
chrome --headless=new --disable-gpu --hide-scrollbars \
  --virtual-time-budget=15000 --window-size=1240,1754 \
  --force-device-scale-factor=2 --screenshot=out.png flyer.html
```

```python
# ואז PIL נותן עמוד A4 מדויק
Image.open("out.png").convert("RGB").save("out.pdf", "PDF", resolution=300.0)
```

לרקע שקוף (אייקונים): `--default-background-color=00000000`.

הגופנים המקומיות במכונה כוללות **Frank Ruhl Libre** אבל **לא** את
Secular One / Assistant / Rubik — לכן מסלול ה־screenshot הוא היחיד
שמשמר את הטיפוגרפיה של המותג.

---

## חתימת המייל

`signature.html` מרנדר את החתימה ומעתיק אותה כ־HTML עשיר בלחיצה,
להדבקה בהגדרות Gmail.

- **Gmail לא טוען webfonts בחתימות.** הכל Arial; המיתוג מגיע מהלוגו
  ומהצבעים.
- הבלוק חייב להיות **`<table>` עם סגנונות inline** — העורך של Gmail
  מוחק כל דבר אחר.
- **ארבע שורות בלבד.** גרסה ראשונה יצאה גדולה מגוף ההודעה; זו טעות.
  לוגו ב־185px.
- אייקונים במקום המילים "טלפון"/"וואטסאפ", עם `alt` כדי שהמשמעות תישמר
  כשלקוח דואר חוסם תמונות.
- **שתי ההפניות הן שורות עם פס זהב בהתחלה** — `border-right:3px solid
  #C9A24B` על `<td>` בטבלה מקוננת, טקסט נייבי 15px מודגש. בלי מילוי
  ובלי מסגרת.
- **כפתורים מלאים נדחו — פעמיים זו כבר תבנית.** גרסה עם שני מלבנים
  ברוחב 300px ורקע `#FBF3E2` הוחזרה עם "מדי בולטים! ומרובעים". החתימה
  חייבת להישאר קלה מגוף ההודעה; כל טיפול שמוסיף שטח צבע נכשל כאן.
  אם בכל זאת צריך פינות מעוגלות: `border-radius` מחייב טבלה מקוננת עם
  `border-collapse:separate` — בחיצונית, שהיא `collapse`, הוא מתעלם.
- **המילה "אתר" לא מופיעה בתוויות.** הקישורים נקראים "פרסום במדריך תשפ״ז"
  ו"אלפון התושבים — רישום ועדכון".
- **פרוקסי התמונות של Gmail שומר במטמון לפי כתובת.** דריסת קובץ בשם קיים
  לא תגיע למי שכבר ראה אותו — לכן `signature-logo-v3.png` ולא החלפה בשם.
- **לא מוסיפים שורות ריקות בתוך החתימה** — Gmail מוסיף את הרווח לבד.
- אפליקציית Gmail בנייד מחזיקה חתימה נפרדת, טקסט בלבד.

---

## כללי עבודה

- **עברית מפוסקת ואוורירית** בכל טקסט שמופנה למשתמש — לא בלוקים דחוסים.
- **לאמת מול החי אחרי כל דחיפה.** GitHub Pages משתהה.
- **קובץ עם עברית שנכתב מ־PowerShell חייב BOM**, אחרת הוא נקרא כ־CP1255
  ומשתבש בשקט. ב־Python: `encoding="utf-8-sig"`.
- **heredoc לא מצוטט ב־bash מרחיב `$` ובקסלשים** ושובר קוד. להשתמש
  ב־`<<'EOF'` תמיד.
- **סוכני ביקורת עלולים לערוך קבצים במקביל** — להריץ אותם קריאה־בלבד.
